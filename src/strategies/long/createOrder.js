
export const createOrder = async(orderData, auth) => {
  const { price, symbol, side, clientOid, size, timeInForce } = orderData;
  // get price data from api

  //store in db

  //fetch data from db
}